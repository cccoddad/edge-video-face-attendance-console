# 项目级工作要求

## 开始工作

- 只在当前项目根目录及其子目录中操作。开始前检查 `git status`、当前分支、最近提交，并阅读 `README.md`、`doc/00-文档目录.md`、`doc/01-项目实施记录与下一步.md` 和相关测试说明。
- 保留工作区已有改动。未经用户明确要求，不得使用 `git reset --hard`、`git checkout --`、强制覆盖或删除与当前任务无关的文件。
- 优先使用现有 Qt5、qmake、PowerShell、OpenCV 和 SeetaFace 工具链；不要擅自切换 Qt6、CMake 或替换算法 SDK。

## 实施与验证

- 只实现当前任务必需的改动，避免无关重构。使用项目已有风格和抽象。
- 修改前说明目标；修改后说明改动原因、影响和验证结果。未构建、未测试或未联调的能力必须明确标记为未验证。
- 代码变更后按风险运行构建、测试或启动验证。遇到问题先进行只读排查，不能自行解决时提供错误证据和最小所需协助。
- 新增的用户数据、模型、抓拍、数据库、日志、测试视频和发布产物必须放在本项目的本地数据/忽略目录，不得纳入版本控制。

## Git 与公开仓库

- 提交前检查 `git diff`、`git status` 和暂存清单，排除密码、Token、个人照片、模型、DLL、SQLite、日志、构建目录、发布目录及机器私有配置。
- 一个可构建、可验证的功能闭环对应一次提交；不要为单个文件制造零碎提交。每个闭环验证通过后必须依次执行 `git diff`、`git diff --check`、`git status`，确认不含隐私数据、构建产物或个人配置，再提交并推送到 `origin/main`。禁止 force push。
- 不得修改其他仓库的远程地址、分支或 Git 配置。若目标目录本身不是 Git 仓库，必须明确说明不能提交/推送，不能虚构 Git 证据。

## RK3568 协作边界

- 必须遵守 `.cursor/rules/rk3568-collaboration-boundary.mdc` 与 `doc/05-RK3568协作边界.md`。
- Qt 项目是 Windows PC 上的下游只读客户端。不得操作、读取、修改、构建或部署 RK3568 网关项目、硬件、服务、端口、NPU、摄像头、麦克风或板端文件。
- 当前使用本地视频文件开发。真实 RTSP 联调必须等待网关验收接口；任何可能影响跨项目接口或板端运行的操作，都要先说明影响并等待用户明确确认。

### 2026-09-04 用户明确授权的例外

- 用户已明确授权：可读取、修改、构建和验证指定的 RK3568 网关项目，优先修复 MPP H.264 编码，并在原始 H.264 输出验证成功后实现只读 RTSP 输出。
- 该例外不允许删除、覆盖或清理既有网关成果；不得停止既有实例、修改其部署目录、重置 Git 或假称未验证能力已完成。测试必须使用唯一的配置、日志和输出文件名，并先检查进程和端口。
- 网关源码当前不在 Git 仓库中时，必须在交接记录中保留路径、已验证证据、未完成改动和风险；Qt 仓库的文档提交不能替代网关源码提交。

## 强制沟通说明

- 每完成一次完整的思考、检查、修改、构建、测试或 Git 操作后，必须立即用中文说明：做了什么、为什么这样做、实际证据/结果、以及本次涉及专有名词的一句通俗解释。未验证必须直接写“未验证”。
- 需要用户操作时，必须一次给出运行位置、完整可粘贴命令、目的、前提、预期结果和失败时需要提供的完整输出或截图；不得把关键命令隐藏在思考过程或拆成零散步骤。

## 交付汇报

完成阶段后简要报告：完成内容、修改文件、验证结果、剩余风险、Git 提交/推送状态和下一步建议。
